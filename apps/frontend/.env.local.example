# URL the browser should hit for API calls and SSE.
# Local dev: http://localhost:4000 (matches backend's next dev --port 4000)
# Production: https://your-backend-domain
NEXT_PUBLIC_API_URL=http://localhost:4000
